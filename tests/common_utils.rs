// common::batch_add / batch_clear are now pub(crate) — internalized into
// the context actor. These tests are no longer applicable as public API tests.
// The batch operations are exercised indirectly through sequence/context tests.
