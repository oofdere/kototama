// Batch is now pub(crate) — its functionality is internalized into the
// context actor. These tests are no longer applicable as public API tests.
// The batch operations are exercised indirectly through sequence/context tests.
