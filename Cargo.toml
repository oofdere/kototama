[workspace]
members = [
    "llama-sys",
]
resolver = "2"

[package]
name = "rusty-llama"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
llama-sys = { path = "llama-sys", features = ["native"] }
